# Known Limitations

As of the 2026-07-04 D1L COM12/COM16 production-roadmap checkpoint:

- Manual visual confirmation of display bars and touch target movement is still pending; the serial `display test` and `touch test` commands return OK on hardware. The COM12 hardware pixel-capture path is implemented and reconstructed the previous PR #33 480x480 RGB565 Home screen as PNG/raw artifacts (`c6a88e2` / PR Actions `28725692751`, merged as `e086312`), including a passing simulator/reference diff. PR #37 changes the Home layout, so the newest Home screen still needs fresh COM12 pixel/manual evidence once COM12 is available.
- MeshCore Public group text TX/RX and signed advert TX/RX are implemented and validated through the serial console, firmware-local contacts can promote heard nodes by fingerprint, full advertised public keys are retained for DM targeting and have passed reboot persistence on `COM7`, contact favorite/mute flags persist across reboot, Public unread/read state persists across reboot, per-thread DM read cursors are implemented, firmware-local routes are learned from Public/advert path metadata, route detail diagnostics and a first route detail sheet are implemented, and serial DM flood TX plus the bounded DM store are validated. DM ACK/PATH receive parsing, direct-route TX selection, contact-row DM compose, first contact detail actions, a bounded Public History/search sheet, and a bounded scrollable DM thread/detail history sheet are implemented, and targeted outbound hardware DM receive by the local COM11 Meshcorebot has passed. `scripts/rf_full_acceptance_d1l.py` now provides a single COM12-side artifact path for identity, Meshcorebot status, outbound DM, controlled inbound DM, ACK/PATH, direct-route, health, and no-Public-command proof. Manual touch review, a passing full RF acceptance artifact, controlled inbound DM unread proof, and physical DM workflow review are still pending.
- Settings are implemented as an NVS-backed firmware model and have passed reboot persistence smoke on real D1L flash. The persisted radio profile is now exposed through `settings get`, serial radio setters, and a touch Radio Settings sheet; Settings now opens as an end-user setup dashboard for SD Card, Wi-Fi, BLE, Radio, Map Tiles, Display, Identity, Diagnostics, About, and Advanced. The radio editor slice has passed hardware flash, standard smoke, targeted serial profile proof, and active Public `test` RF regression on `COM7`; the current dashboard slice is host/simulator covered and awaits Actions-built COM12 hardware flash plus physical touch review.
- First-boot onboarding now persists a setup-complete flag, collects the node name, confirms Canada/USA defaults, keeps Wi-Fi/BLE/observer disabled, and generates/retains the local MeshCore identity. Optional-radio choices in onboarding remain limited until live Wi-Fi/BLE runtime support is enabled.
- `identity status` now generates and reports an NVS-retained Ed25519 local identity fingerprint. This is a minimal firmware identity model, not the final full MeshCore C++ store integration.
- MeshCore TX timestamps are retained monotonically in NVS to avoid duplicate filtering, but a real RTC/network time source is not wired yet. The current production patch keeps DM/Public TX from failing under onboard NVS capacity pressure by falling back to a monotonic RAM timestamp if only the timestamp persistence write fails; that fallback must be hardware-retested from the next Actions-built firmware package.
- MeshCore 3-byte companion framing is implemented and host-tested, but the live binary bridge to MeshCore protocol frames is not enabled in Phase 1.
- Packet log storage keeps a 128-row active RAM ring and persists the newest 8 rows through the compact NVS fallback. When the RP2040 SD file protocol is ready, packet history uses a split retained-store write path: SD receives the primary snapshot through coalesced flushes while NVS keeps the newest fallback rows, and a validated 4096-record segmented SD journal backs 100-row Packet tab pages with Load Older/Newer. Packet rows carry a bounded raw hex preview and expose serial filter/search/raw commands plus a touch terminal-style Packet tab with RX/TX/fail/error coloring, pause/resume, filter/search, route evidence, and normal/advanced packet detail; full hardware proof of 24h SD scrollback and deeper protocol decode are still pending.
- Optional SD-card data storage is now FAT32-only and non-destructive. Current ESP32 firmware reports safe `storage status`, explicit non-formatting `storage mount`, non-formatting `storage diag`, `storage setup` with `policy="no_device_format"`, and a touch SD Card sheet. The RP2040 bridge has no SD formatting command; users must provide FAT32 cards prepared on a computer. On mounted FAT32 media, DeskOS creates missing `/deskos` folders/manifests and can switch retained Public/DM message history, route history, and packet history to SD through `DESKOS_SD_FILE v=1` when the file-operation gate is ready. Serial storage canaries cover file writes, exports, sampled data export, synthetic map-tile cache commits, and a bounded allowed-provider HTTPS tile download command without Public RF or formatting. Touch Map Tiles setup can persist an allowed provider/attribution/zoom, but live touch downloads remain unavailable as `tile_render_pending` until tile rendering proof exists. Settings, identity, contacts, read-state, and boot-critical state remain onboard/fallback-backed. GPS/location-source integration, browser/geolocation integration, nearby GPS node pins, SD tile rendering/sideloading, and hardware proof remain pending.
- Older guarded-format hardware artifacts from July 1-2, 2026 are now obsolete for release readiness because the public policy forbids on-device formatting. Fresh SD acceptance evidence must use a FAT32 card prepared on a computer, prove auto-creation of the DeskOS directory structure, prove no-card NVS fallback, and prove non-FAT32/unmountable cards remain non-destructive with clear repair guidance.
- Current COM12/COM16 evidence from `1a29876` / GitHub Actions run `28714355561` proves the core SD path on the installed FAT32 card without Public RF or formatting: the official Seeed SD smoke passes on COM16, COM12 smoke reports `READY_SD`, raw RP2040 diagnostics complete, the file canary passes, correct/missing/existing-data boot scenarios pass, RP2040-unavailable fallback is captured after official smoke, retained Public/DM/route/packet history survives reboot, reboot/remount passes, the synthetic map-tile canary passes, and export canary/diagnostic export/sampled data export all pass. Public release is still blocked on the remaining physical SD evidence matrix: actual no-card and unformatted/non-FAT32 scenarios, FAT32-only rejection behavior for unusable media, <=32GB multi-card proof, no-format guidance evidence tied to unusable media, and power/electrical evidence.
- Phase 6 mesh visibility now exposes `signal`, `roomservers`, `repeaters`, `routes trace <fingerprint>`, and an opt-in DM-only `routes probe <fingerprint>` diagnostic plus Home/Packet-tab summary cards. A first touch Mesh Roles browser sheet and contact Route Trace sheet with `Ping` are implemented, and newest-build active-probe hardware proof passed in `artifacts/hardware/com12/routes_probe_a1afd4b.json` with no Public RF. Manual physical touch confirmation of opening/scrolling/closing these sheets and richer filters are still pending.
- Contact export now emits MeshCore-compatible `meshcore://contact/add?...` payloads over serial and adds a Contact Detail `Export` action with LVGL QR rendering enabled in the default build. It requires a promoted contact with a retained 64-hex public key. The QR-enabled build passed COM7 flash, standard smoke, targeted `contacts export <fingerprint>` proof, and active Public `test` RF regression; manual touch open/close review and scan/import proof with a MeshCore client are still pending.
- The Phase 3/4/5/6/D/E touch shell is implemented with first-boot onboarding, a SiguredOS-style title-only icon Home launcher, Public `test` action, first Public free-text composer, paged Public History/search with Load Older, first contact-row DM composer, first contact detail sheet with DM/favorite/mute/Trace/Edit actions, a contact Edit sheet for alias rename and promoted-contact Forget, paged DM thread/detail retained history with Load Older, `Reply`, and per-thread `Read`, persisted recent Public/DM preview rows, Public/DM unread counters, newest contact/heard-node rows, Home Time/Wi-Fi/BLE/SD status icon routing, an offline Map tab with SD tile-cache policy/readiness, retained route evidence, serial-configured manual center display, first-open touch latitude/longitude D1L pin entry, touch Map Tiles provider/attribution/zoom setup with live downloads unavailable as `tile_render_pending`, first Mesh Roles browser sheet, first contact Route Trace sheet with DM-only `Ping`, Packet-tab route rows with a first route detail sheet, terminal-style Packet-tab rows with filters/search/pause and normal/advanced packet detail, Settings setup dashboard tiles for SD Card/Wi-Fi/BLE/Radio/Map Tiles/Display/Identity/Diagnostics/About/Advanced, a Radio Settings sheet, Storage Setup sheet, Wi-Fi scan/connect setup sheet, staged BLE setup sheet, staged Display sheet, Diagnostics sheet, advert sheet, toasts, and lock overlay. Previous COM12 Home pixel capture is complete on PR #33 (`c6a88e2` / PR Actions `28725692751`, merged as `e086312`); PR #37 host/simulator and Actions evidence cover the newest Home layout, but fresh COM12 pixel proof is pending. Targeted UI corruption probe and scroll probe are complete on `59610ab` / Actions `28723265336`; PR #35 / Actions `28727064923` closes the all-target keyboard/sheet proof with 12 COM12 captures and `public_rf_tx=false` / `formats_sd=false`. Navigation, modal helper, shared chrome/layout policy, compose-probe keyboard target/geometry policy, and screen dispatch/content-root cleanup boundaries are split, but #16/#6 still need remaining owned screen/render modules, residual keyboard widget wiring, deeper single-root invariants, and current COM12 Home/manual proof. Nearby GPS node pins, GPS/location-source integration, actual tile rendering/sideload browsing, BLE companion transport, Display runtime persistence, OTA, and manual physical review of the newest touch surfaces are not complete yet.
- `tools/ui_simulator.py` now provides deterministic 480x480 host screenshots, schema-v2 required-label/text-overflow checks, a touch-target map with expanded 44x44 boxes, expected flow coverage for onboarding, lock/unlock, Public compose/history/search, DM thread read/reply, contact detail/edit/export/trace, Map policy/location picker/manual center, packet search/detail/routes, Mesh Roles, Radio, Storage, Display, Diagnostics, and Advert, plus RF/destructive/no-format action flags. It also includes storage-state scenarios, a `manual-location` scenario, and a `large-mesh` stress scenario for oversized node/message stores. This is a fast CI regression check, not a replacement for the pending manual physical-screen review of touch behavior, LCD color/brightness, animation smoothness, touch-controller accuracy, or genuinely large live RF mesh behavior.
- A UI refresh fix has been added for the reported split-page/live-packet corruption: RF-mutated packet/message/DM/node/route/contact stores and mesh-inspector scratch buffers serialize reads and writes before UI snapshots, touch callbacks now request coalesced content refreshes instead of rebuilding the active tab inline, and live data generation changes queue redraws for the UI task. The LCD path has a firmware-maintained capture shadow; `59610ab` / Actions `28723265336` evidence passes the targeted UI corruption probe, PR #33 (`c6a88e2` / PR Actions `28725692751`, merged as `e086312`) passes the previous Home pixel-capture simulator diff, and PR #35 / Actions `28727064923` passes the all-target keyboard capture. Final release still needs physical photos/manual review, fresh Home pixel capture for PR #37, full RF acceptance, remaining SD matrix evidence, remaining #16/#6 UI modular ownership evidence, and 12-hour soak.
- The Public composer, paged Public History/Search sheets, contact-row DM composer, contact detail sheet, contact Route Trace sheet, contact export sheet, Radio Settings sheet, paged retained-history DM thread sheet, per-thread DM read action, Messages unread/read action, route detail sheet, and packet detail sheet have passed host contract/simulator tests. Public History/Search has also passed older hardware flash, standard smoke, and a targeted COM11 direct-message regression without Public-channel RF; the newest Load Older behavior still needs COM12 validation from a downloaded GitHub Actions artifact. The contact export, radio settings, per-thread DM read, and retained-history DM thread additions have also passed hardware flash, standard smoke, targeted serial proof, and active Public `test` RF regression on `COM7`. Manual physical touch entry on the composer keyboard and public-history/thread/reply/history/contact/export/radio/read/route-trace/route-detail/packet-detail surfaces is still pending.
- The SX1262 Phase 1 probe intentionally avoids TCXO/DIO3 control. TCXO remains `NONE` by default.
- Wi-Fi and BLE are managed by an offline-first companion-radio policy. Wi-Fi remains off by default, but the Phase D station path can be explicitly enabled for bounded `wifi scan`, saved-profile `wifi connect`, and touch Wi-Fi Setup use without printing saved passwords. BLE companion transport remains build-disabled/pending. Wi-Fi behavior must be validated on hardware from downloaded GitHub Actions artifacts, not from local firmware builds.
- Phase 7 diagnostics now expose a small reset/crash ring and richer health telemetry. A repeatable soak runner now samples health, mesh, signal, unread, packet, command retries, and crashlog state for idle/listening or active Public `test` windows, and a retry-aware 1-hour active Public `test` hardware soak passed on `COM7`; the full 12-hour idle/listening soak is still pending.
- `scripts/release_gate_audit_d1l.py` now provides a machine-readable release gate audit. Hardware evidence gates are commit-metadata strict so stale UI, scroll-probe, outbound-DM, RP2040/SD, RF, manual-review, or soak artifacts from an older flash cannot pass a final release audit even when filenames are misleading. The SD gate also rejects obsolete format-capable preflight guidance and requires no-device-format evidence (`formats_sd=false`) before SD acceptance can pass. UI proof is split into issue-sized gates: targeted UI corruption and scroll probing are proven on `59610ab` / Actions `28723265336`; issue #2 all-target keyboard capture is proven on PR #35 / Actions `28727064923`; previous Home pixel capture is tracked separately by PR #33 (`c6a88e2` / PR Actions `28725692751`, merged as `e086312`). Release remains fail-closed until RF/DM proof, remaining physical SD matrix proof, 12-hour idle/listening soak, manual physical UI/photos, newest Home pixel capture, and remaining #16/#6 UI modular ownership evidence pass.
