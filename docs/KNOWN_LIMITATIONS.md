# Known Limitations

As of the 2026-07-16 strict-banked PR #160 exact-main checkpoint:

- Live `main` is strict-banked through PR #160 at `5ee89e3d3956e39cd8eff4c87374d22b3f40613b`. Exact-main Actions `29460262807` pass 1,166 host tests, 32 checksum-contract tests, 32 focused retained-time tests, 1,008 wire vectors, 931 oracle checks, 100,000 wire plus 100,000 advert fuzz executions with zero findings, and five API ZIPs / 46 entries across 339 files / 79,775,680 bytes. Strict receipt SHA-256 is `dad1a84d81b45a7c5c94e51e2ffb1e8cd28febd4f4f98a4d6a862566aeb38a91`; gate-manifest SHA-256 is `214deb9d1f74952cec7784de15154e2c36b5cecf5b9de1b9208763c423569284`. Its dry audit remains fail-closed at 35 failures overall / 33 P0 (audit SHA-256 `b128439541a559b6e3ddd05cfbb95827fc5b7415753d4bc9741633b10e5462bd`). Provenance is valid but unauthenticated and no SLSA level is claimed.
- PR #158 banks fail-closed versioned settings durability at exact main `3de6e8d634bd76fc52e5e4bf98f93fc48adf6648`; portable aggregate SHA-256 is `eb82dd9f8763812597f6d9dda0c3e783594c5666b3b23abb8c5375670c1fbb2e`. Full-reset, injected power-loss, endurance/write-amplification, exact-candidate physical durability, and WP-11 closure remain open.
- Corrected PR #159 banks the compact 44-pixel icon-plus-text dock, `Nodes`/`Tools` naming, shared chrome geometry, and Map bounds at exact main `fada8311a6e63acb7fd4f791478660ce3926c48a`; portable aggregate SHA-256 is `855ee4c118df03758d38f546cd7fff8fab9ee711b2758701f57d7fe6ac0be34b`. Rejected predecessor `31c5799e413ebaab218c5c9d01c88172f8a33185` remains negative evidence because it left a 10-pixel Map strip; blocker-receipt SHA-256 is `fb6a6e564fe2d67ae2fd2af815b147578ee28a47d1ba604b096b5dae1e232ed6`. Physical touch/pixel/accessibility review, 1,000 transitions, lifecycle proof, `ui_runtime_safety_<sha>_<d1l>.json`, Home status-icon work, WP-14, and release closure remain open.
- PR #160 banks a versioned, domain-bound, CRC-protected retained-wall checkpoint in dedicated NVS, approximate display/order recovery that can never become certificate-valid or protocol-authoritative, protocol-guard rollback rejection, retained-worker writes with generation protection, six-hour material-change coalescing, bounded retry, record preservation, and truthful telemetry. Portable aggregate SHA-256 is `15ff143e6e226201017fc3d34fd5e4c12c52ac2e4de8bd1271b2ca473222bc2b`. Legacy `mesh_ts` migration, timezone display conversion, authenticated companion transport acceptance, exact-candidate physical SNTP/jump/TLS/reboot/power-interruption evidence, `time_service_acceptance_<sha>.json`, WP-12, and release closure remain open.
- PR #157's secure channel-management sheets and fail-closed entropy service remain banked software/artifact proof only. Official-client RF, exact-candidate channel reboot/reset/recovery or physical UI proof, `channel_acceptance_<sha>.json`, dependency closure, WP-09, and release closure remain open. WP-01 remains banked on predecessor source `092293f2311a24c9899bc9bf343ab014c4ba0411`; WP-02 frozen-candidate physical roles remain deferred.

- PR #123 makes authenticated retained PATH learning, current-boot freshness selection, direct DM/ACK dispatch, and one bounded flood fallback available in merged software. Its portable aggregate SHA-256 is `49dcb153fb71b5d01019c9ac0b9a5df0b73551816773155978cafa214e516b8d`. The software status is `merged_in_progress`, but host, oracle, fuzz, and artifact proof cannot substitute for unsatisfied prerequisites or the still-required exact-candidate `route_trace_acceptance_<sha>_<d1l>_<peer>.json` and RF/physical evidence, so the package remains `blocked` rather than complete.

- PR #124 binds the protocol allocator floor to clean exact-source commit time, rejects dirty/historical provenance overrides, quarantines excessive SNTP forward jumps before allocation, permits representable authenticated companion recovery, preflights protocol time before identity/RX/radio/message-store side effects, and exposes persistence/admission recovery diagnostics. The strict-banked portable aggregate SHA-256 is `bd22c908edf661cff9aefb7e4b155e4f6b6e2d5c682e29bee6f3c7cce9f46851`. PR #160 subsequently adds bounded retained validated-wall checkpoint/recovery; explicit legacy migration, timezone/display conversion, authenticated companion transport proof, physical SNTP/jumps/TLS/reboot/power behavior, the required `time_service_acceptance_<sha>.json`, and WP-12 closure remain open.

- PR #125 moves Home truth into a pure immutable-input view model, gives the Home controller an owned render snapshot plus stable event bindings, clears handlers/bindings on deactivate, saturates unread aggregation, and removes raw app-snapshot reads from the renderer. Retained PR Actions `29378595462` pass 1,038 host plus 32 checksum tests and 142 simulator views; exact-main Actions `29379361608` and strict receipt `51375044adfa8edd95888e1950d0b8f030719eb15d6450aa8022990269e3b721` bank the merged software/artifact state. Complete UI-task/command ownership, remaining screen extraction, lifecycle/generation/redraw invariants, 1,000 transitions, physical UI, the required `ui_runtime_safety_<sha>_<d1l>.json`, and WP-14 closure remain open.
- PR #126 adds the first bounded WP-13 Wi-Fi resilience slice: the explicit ten-state policy, bounded exponential retry with jitter/attempt/window ceilings, auth/AP/transient classification, user cancel/disable, one-shot intentional-leave suppression, serialized control, retry-task telemetry, and boot-local safe-mode containment. Exact-main Actions `29380830171` and strict receipt `b4451d35cff999ae84d13159b78faa2e3d1bab69594f2cd2054aef4bf531a741` bank software/artifact proof only. PR #128 subsequently banks persisted repeated-crash attribution; the credential physical-flash threat model/NVS-encryption decision, exact-candidate live AP/reboot/wrong-password/weak-signal/safe-mode/full reconnect-stress acceptance, the required `wifi_resilience_<sha>_<d1l>.json`, and WP-13 closure remain open.

- PR #127 moves More truth into a pure bounded view model and gives its controller an owned render snapshot plus generation-checked category/action bindings that are cleared on deactivate. Exact-main Actions `29382094865`, strict receipt `a239057b122b20fcfe48e7f98cff32c0274deb85f11708698d802c62baa0c848`, and portable aggregate SHA-256 `963a2d9ea68e7b0fb8935ab13e8242a6c062f94e316fd6851ae85f0b6208288e` bank software/artifact proof only. PRs #129 and #130 subsequently extract Storage and Wi-Fi setup ownership; remaining route/contact/Map/Terminal/admin/update extraction, complete UI-task/command ownership, lifecycle/generation/redraw invariants, 1,000 transitions, physical UI, the required `ui_runtime_safety_<sha>_<d1l>.json`, and WP-14 closure remain open.

- PR #128 merges and strict-banks a versioned, CRC-protected persisted repeated-crash boot guard with crash-like reset attribution, a saturating consecutive-failure threshold, fail-safe invalid/unavailable-record handling, explicit disable/profile-clear disarm, and stable-connection acknowledgement. Exact-main Actions `29382842140`, receipt SHA-256 `84082900a8c8e973729351e2329f6f004ac33a6ec6b12bb1409a0e27bb6ef223`, and portable aggregate SHA-256 `97b2f89f762f8acbdaa84e7d8b905e2dbde58bdd43584771d10345a2c96ca463` prove only the software/artifact slice. The credential physical-flash threat model/NVS-encryption decision, exact-candidate live AP/reboot/wrong-password/weak-signal/saved-profile/safe-mode/full reconnect-stress acceptance, the required `wifi_resilience_<sha>_<d1l>.json`, physical evidence, and WP-13 closure remain open.
- PR #129 merges and strict-banks the seventh bounded WP-14 extraction: a pure size-bounded Storage view model and owned render snapshot distinguish no-card, ready, reconnecting, degraded, FAT32-required, and per-backend location truth; output is validated and the read-only/no-device-format boundary remains explicit. Exact-main Actions `29383558565`, receipt SHA-256 `c9417c610da53ce718ff5afc829d9525560eb15055699173d0bbbd2304101d85`, and portable aggregate SHA-256 `c377f329c9238e9b608c2d2581a0532106918f61d5d53e27125a4195d1795810` prove only the software/artifact slice. Physical SD/UI evidence, remaining UI ownership/lifecycle/1,000-transition gates, the required `ui_runtime_safety_<sha>_<d1l>.json`, WP-14 closure, and release closure remain open.
- PR #130 merges and strict-banks the eighth bounded WP-14 extraction: one size-bounded Wi-Fi setup controller owns the sheet, immutable fixed-array render truth, and nine generation-checked bindings; malformed models fail closed, repeat creation destroys stale owners, and password input is never copied into controller/render truth and is cleared before refresh, hide, or destruction. Exact-main Actions `29384334171`, receipt SHA-256 `160b848704a839e14424086be9b19c96124fb79f6c3b7794ba0f24359d444e75`, and portable aggregate SHA-256 `86137f1f43b518c5a5d0136aea0249f63e4eeb842e4041e7c371c8cafd5ce81f` prove only the software/artifact slice. Credential flash threat-model/NVS-encryption, live AP/reboot/wrong-password/weak-signal proof, physical touch/keyboard/credential-memory evidence, remaining UI ownership/lifecycle/1,000-transition gates, WP-13/WP-14, and release closure remain open.
- PR #131 merges and strict-banks the ninth bounded WP-14 extraction: one bounded external-RAM owner controls the Map location/options sheets, nested page, controls, render/probe/modal access, repeat-create cleanup, and keyboard detachment; partial allocation and stale ownership fail closed. Exact-main Actions `29384981503`, receipt SHA-256 `86f5bed6b506a1a2ebce9dc701bba33ba428560fef7a5b8f7fa8038d1ad5191d`, and portable aggregate SHA-256 `3fb7490733c6a449c5c11b738a40252c029dbc9653c276e8ab79c7cc7cb863f3` prove only the software/artifact slice. Map provider/cache/fetch/render/cancel/revisit lifecycle, GPS/BLE, physical UI, remaining ownership/lifecycle/1,000-transition gates, WP-14, and release closure remain open.
- PR #132 merges and strict-banks the tenth bounded WP-14 extraction: one bounded external-RAM controller owns the BLE setup sheet, rendered snapshot, generation-guarded close/toggle actions, and fail-closed lifecycle while unavailable pairing/forget controls stay disabled. Exact-main Actions `29385596543`, receipt SHA-256 `f73699d3862e4be6557d396bc6ac1ac77c249f7463bcc7c079fe3ad080de3323`, and portable aggregate SHA-256 `a780825369e3b5f9663f6f8c42869f44a8a176e72f66e145c972998dd0753c47` prove only the software/artifact slice. Live BLE transport/pairing, physical UI, remaining ownership/lifecycle/1,000-transition gates, WP-14, and release closure remain open.
- PR #133 merges and strict-banks the eleventh bounded WP-14 extraction: one small external-RAM controller owns Display and Diagnostics sheets, atomically replaces both with partial-failure rollback, generation-guards close actions, and keeps diagnostics snapshot-only and staged actions disabled. Exact-main Actions `29386120240`, receipt SHA-256 `0ae499707e8b4beedcb21c52507cec18ba2c9ddb4edaf0de3480b47012601f7b`, and portable aggregate SHA-256 `d6006a2690cbc2e1484d7d5bbc9b7779a87a2dc8ab7f82fed82e60a62ac0256f` prove only the software/artifact slice. Live display persistence and diagnostic export/soak actions, physical UI, remaining ownership/lifecycle/1,000-transition gates, WP-14, and release closure remain open.
- PR #135 merges and strict-banks the twelfth bounded WP-14 extraction: one bounded external-RAM controller owns the Radio Settings sheet, edit snapshot, safe adjustment bounds, generation-guarded touch actions, and fail-closed lifecycle while application mutations remain in Phase 1. Exact-main Actions `29386710505`, receipt SHA-256 `9be0a793c13988511b2c897134d1aa508064149933e3abf68adf394bd59fa55e`, and portable aggregate SHA-256 `698215a2fbfefd9d9a80917e74f9a447d4baeeed1bb4a07c941aeeaf0019101e` prove only the software/artifact slice. Live RF apply/persistence, physical RF/UI, remaining ownership/lifecycle/1,000-transition gates, WP-14, and release closure remain open.
- PR #136 merges and strict-banks the thirteenth bounded WP-14 extraction: one bounded external-RAM controller owns Contact list/detail sheets, generation-guards navigation, and fails closed during replacement/destruction. Exact-main Actions `29388000211`, receipt SHA-256 `06ce2689da51933e99b4c3222f0714af1f4a639974ba4d779ffb171f3a68ead2`, and portable aggregate SHA-256 `f3e3e2d4c62720c22bb5d7c6b9fc6fd61d5e53d13ea938815e4460d773644d9e` complete the planned hierarchy/controller extraction in code. WP-14 still requires UI-task-only LVGL proof, repeated open/close lifecycle proof, 1,000 transitions, exact-candidate touch/scroll/focus/keyboard qualification, and `ui_runtime_safety_<sha>_<d1l>.json`; no physical or release closure is claimed.
- PR #137 merges and strict-banks the first bounded WP-15 slice: one owned retained direct-message thread controller generation-guards actions, renders directional wrapped bubbles, exposes persisted delivery state and technical detail, refreshes on content revision, marks read once on open, and keeps navigation public-RF silent. Exact-main Actions `29389463246`, receipt SHA-256 `605cc5d8988eb798f4825553314535c5f43ee54993ebddc7cbad7f77cb1bde71`, and portable aggregate SHA-256 `6603a075eb20f254c7ee4b30a9c7b5ae00beca8b40b5c7de8aeeb7b5f3fe4c1b` bank only this partial software slice.
- PR #139 head `d8457e2578a5ae0eb8f9cee0596d53f68bcfd7d6`, retained synthetic merge `485d6d014388d3e00a81786db1e95012f7302b95`, and merged main `840ae4874ad204d8fe13a29ca2e832fc0961d155` share tree `9bf1c86026c39d61e661fab74c48603c9177653b`. Retained PR Actions `29391179655` pass; duplicate push run `29391170573` was cancelled and excluded. PR receipt SHA-256 is `9fc294d0d212cd53c5a41af8a37ff8214e11eb4d165c1316dc97ceff17506d5e`. Exact-main Actions `29391670113` pass 1,094 host plus 32 checksum tests, five ZIPs / 46 entries across 228 files / 75,037,120 bytes, conformance/fuzz, exact-source provenance, and SPDX; exact-main receipt SHA-256 is `d513bed9bb647a48a01bf57ce7266f99e55a755602ca645d4d479b0940143c0d`. Portable WP-15 Messages root/Public chat aggregate SHA-256 is `63f6a69a84b9862524b36dac254a5b941a419e339e5928576d2401b755bcb742`. This banks only the bounded Messages hierarchy/Public chat software slice.
- PR #140 head `1f74451256d8de918581073c1b2a238ea228cad5` and retained synthetic merge `b0f9a0c0c8e3615315690c1c2236d10eedd1ba82` share tree `c0941b6a3e8113556c7afa42c4ff74fc125c4987`; exact main `0980cc8f8142320b014f9a78cae9cd125f430bcc` has tree `4baa4e2da5653a55e38e355aae9e9eb4395a2dd6` because PR #141's completion evidence merged between the original base and PR #140. Retained PR Actions `29393024537` pass; duplicate exact-head push run `29393012868` also finished green and is recorded/excluded in favor of the canonical PR run. PR receipt SHA-256 is `6700e426a4be90d2668df10c119a156e3bdfbbee2fdea3810c4cd6d8f2606e0a`. Exact-main Actions `29393666190` pass 1,098 host plus 32 checksum tests, five ZIPs / 46 entries across 240 files / 75,511,473 bytes, conformance/fuzz, exact-source provenance, and SPDX; exact-main receipt SHA-256 is `a912e8d48fab7764e69d2abd053a3623ae257f1b4c7d1c9be61343c367014619`. Portable WP-15 strict UTF-8 aggregate SHA-256 is `84d4a2f7e60a3d39366afa0b7339f3a898d6cd98527824aedb191239d60e0b0d`. This banks strict printable UTF-8 admission, exact retry/retention/reboot/JSON preservation, canonical plaintext padding validation, and truthful character/byte compose eligibility only. PRs #144/#145 subsequently bank route/contact compose eligibility and bounded retained search; the current residual is stated in the PR #145 bullet below.
- PR #142 head `f3e7f9494b8f6cbdff72b1973625c5f8cf44280f` and retained synthetic merge `4b5a4d8e5cd63fcee6d57e77c2c6ded315987104` share tree `a1affb220fa6a766dabee7ddfb14a5b8a016d680`; exact main `c61978fd3acb738e68cd225d7b74825242757358` has effective parent `cabf0e3ab64ebab14b408bda329ec91c4e1d30e9` and tree `9790a2ce4572488cfaf60f824eaa97eb61126aea` because PR #143's completion evidence merged between the original base and PR #142. Retained PR Actions `29427248379` pass; duplicate exact-head push run `29427244336` also finished green and is recorded/excluded in favor of the canonical PR run. PR receipt SHA-256 is `cca73d0341948d881ebc7ac8f1788451f34a896cbe70abe17d5872b31deb809f`. Exact-main Actions `29429249436` pass 1,103 host plus 32 checksum tests, five ZIPs / 46 entries across 240 files / 75,534,517 bytes, conformance/fuzz, exact-source provenance, and SPDX; exact-main receipt SHA-256 is `378270da5077a7d991ad72ad2d07dd318f8adb2f308f7147553e1e1e976ef38f`. Portable WP-15 DM-conversation-list aggregate SHA-256 is `3943170bfa7415b9b02c4ea9ec69ef22a98047c61a4b06f82aadcaed36bdf6cf`. This banks one newest-first row per exact contact, outbound-only conversation preservation, per-conversation unread truth across visible rows, muted-unread separation, and the durable-ring-plus-volatile-tail read-state boundary only. PRs #144/#145 subsequently bank route/contact compose eligibility and bounded retained search. No hardware or RF closure is claimed by this historical slice.
- PR #144 head `86d1cf25e97d79883979fa5b4f0720a4a8bf67f6`, retained synthetic merge `4140590d493ca16e5529bb8d5d665b383cb83782`, and merged main `3017179bfe0cc5c7f4609a9ca5d5b88b3d229618` share tree `40973e4977855f2f36086c94ff3164aa608a8647`. Retained PR Actions `29429605305` and exact-main Actions `29430568642` pass; strict receipt SHA-256 is `5a13322b075bef165ae5a62fa250755ac50cc3b57ef45d0be68e33c559eebc4d`, and portable aggregate SHA-256 is `904b4403d94098f2573c0cad35fce0a9a967818410e85c8bf69cef7dae94c221`. This banks fail-closed live Public/DM Send eligibility, canonical flood-fallback eligibility, explicit transient retry, persistent failure latches, failed-draft retention, and RF-silent refresh only.
- PR #145 head `8514355b808ea49211e6266b2a05a35c3f1e1a59` and retained synthetic merge `24214d8d43e177581707ef07866fb3cdc5a0ff17` share tree `46da0e56c7f6dde0744a910d921e25fdd02632c0`; exact main `242c5aa455cbbe9bf0b1b4cbfe4aa8bb8e52ab7f` has effective parent `870a5483ee9795927877e3045f0bc5f53c595a71` and tree `393596c8564f80704c46ccd4248f5efaaeda62fc` because PR #146 completion evidence merged between the original base and PR #145. Retained PR Actions `29431926359` and exact-main Actions `29432646761` pass; strict receipt SHA-256 is `3626b8bf73ff584977811f841f0d9c79360da053ec351aab129a27311f435d23`, and portable aggregate SHA-256 is `392b5a73ff147d6df2ff632451379c2582a2571276e6971a32c37750148254a8`. This banks bounded exact-fingerprint, thread-scoped, read-only retained search, match-relative paging, truthful empty/no-match states, and a refresh-stable modal only. PRs #147, #149, and #150 subsequently close the remaining scoped software behavior; the current residual is stated below. No hardware or RF closure is claimed by this historical slice.
- PR #147 head `beeaa119fce9dfea79e2d4d52985eb1b9b9e083e` is strict-banked on exact main `70baf8ca5689ada80bceaa7c7ad1fae53abf6678`. Exact-main Actions `29435863354` pass; strict receipt SHA-256 is `28530db268638d3bd29908093ead458c106161ba62ab62955d6af041fc3de719`, and portable sender/DM-identity aggregate SHA-256 is `dbc95d295b17c3c7eef718e9ea786a043a449bc1caafeff78bcf6ce48284ffb4`. This banks exact sender-to-thread identity explanations and fail-closed contact eligibility only.
- PR #149 head `e84d979e12ca6733281d4b0b2b3f21134a258cac` is strict-banked on exact main `e2d4dce8e1d265b196c91cf2e17492e727381e03`. Exact-main Actions `29437496314` pass; strict receipt SHA-256 is `618f3a32e4090712180915fff46ca3265c81a6279d32c83a6fbd9731f26ba1b5`, and portable retained-state aggregate SHA-256 is `c399febd8bf4da0acc3e5113f0c278385ed651c1b627908051a3a280512255b5`. This banks truthful loading/storage-degraded/no-contact states, retained read-state behavior, and trusted/untrusted time presentation only.
- PR #150 head `b218027471ba81cbf7ba02e03d505476c85af278` and retained synthetic merge `b11e5538aaafe35d5d81e822030b7ad1166b47c2` share tree `0f3984f205e3328b17b0b033578ecf077e3a3a36`; exact main is `c20d5b4580188fe86d1607845050f08aa37fec24`. Retained PR Actions `29438062768` and exact-main Actions `29438968424` pass; PR and exact-main receipt SHA-256 values are `85ce8dec862c606dad2a5f6a6c397e3f1e18a7d482cb2a85abef42fdedfb36bf` and `051cef8cd7aaab4b6d1302bca379c39e207dd0651378aa59eef2fd02aff916c7`. Portable cursor/incoming-refresh aggregate SHA-256 is `c4f8499091b113b9cd6179a5083e37c868321ee1c8d90e97bc1f147997c186a2`. The scoped WP-15 software behavior is complete; dependency closure, controlled-peer RF, exact-candidate physical UI qualification, and the required acceptance artifact remain open. Curated emoji/glyph, notification, accessibility, and polish work remains WP-21. No hardware or RF closure is claimed.

- PR #117 makes the runtime task the sole owner of MeshCore request and TX-completion state; persists DM truth through queued, radio, ACK, retry, failure, and reboot transitions; requires an exact nonzero TX operation origin for terminal callbacks; rejects stale callbacks; recovers watchdog timeouts only for the exact active origin after quiescing IRQ/latch state; protects nonterminal outbound rows from ring eviction; and packages the pinned TX-origin patch with SPDX coverage. This is exact-main software/artifact proof, not RF, device, UI, dependency, or WP-07 closure. The portable aggregate SHA-256 is `8cf9f669d8392137db527496bbbd66acc6fa4ccfe84eada335949469f0f8958c`.

- PR #118 gives Public, DM, packet, and route stores one descriptor scheduler while preserving isolated serializers; carries one absolute deadline across request-mutex wait, queue residence, flush-mutex wait, and the descriptor pass; stops before later stores after deadline or quiesce cancellation; reports coherent active/last-pass state; and uses overflow-safe finite tick conversion. An already-running synchronous store callback cannot be cancelled mid-call and may finish after caller timeout, but remains observable, starts no later descriptor, and releases its locks. This is exact-main software/artifact proof, not schema/reset/power-loss, NVS write-amplification/endurance, physical durability, or WP-11 closure. The portable aggregate SHA-256 is `8d2991f4ce60d4482610ba606540bf17f1942d32a8ce93cbdfaba4317e203847`.
- PR #154's retained-NVS telemetry is deliberately boot-local and API-level. Exact main `77c0d5a281c15402fe13e9aae5b9655f177765da` / Actions `29447654377` is strict-banked by receipt SHA-256 `b7000417aae7e1b0142bcfd39b1754352a23ba49a416eb5ef40919a022cd24f3` and gate-manifest SHA-256 `96feaede73487020f3dfd1c62587028b46e09c8989ff28849095a34bcd2384f1`. It reports dedicated-partition entry capacity and successful `nvs_commit` requests globally and per retained store, but it cannot observe the NVS implementation's physical page compaction, flash program operations, erase cycles, or lifetime wear. This enables the required named-workload measurement; it does not close power-loss, endurance, physical durability, or WP-11 acceptance.

- PR #114 adds a bounded durable channel data model: Public plus seven user slots, stable 64-bit channel/history identities, source/default/enabled/unread metadata, redacted normal reads, schema-v2 magic/length/CRC/lineage/generation, v1 migration, reset lineage, locking, and secret zeroization. It intentionally does not wire runtime channel selection, send/receive/history, the channel-management UI, official-client interoperability, or RF/physical acceptance. WP-09 therefore remains blocked and `proof_banked=false`; the portable aggregate SHA-256 is `545e15878bfcfbb41e3c2f9255e857df44f8cfde2faec3e0dcc5080cf58f5b8b`.
- PR #116 adds one locked atomic redacted channel snapshot, atomic persisted default selection with rollback, app-model copy/select APIs, and strict redacted USB `channels` / `channels select` commands. It performs no RF transmission or SD formatting and still does not integrate selected-channel send/receive/history/unread or the channel-management UI. WP-09 remains blocked and `proof_banked=false`; the portable aggregate SHA-256 is `6eb0c786f40acdebfd4140505bf2f89983e796d9f5939ca142d030a43484084f`.
- PR #152 integrates exact channel identity through retained rows, isolated history queries, per-channel unread state, selected-channel TX/RX, fail-closed pending-operation completion, persisted lineage, reboot/rollback/eviction reconciliation, and hot-SD merge while preserving Public compatibility. Retained PR Actions `29444359959` and exact-main Actions `29445202940` pass; exact-main receipt SHA-256 is `a4c9d17636f027cc02212ea68ba2ef0c281313f566b63eb8e9aa688d9c5153d3`, and portable aggregate SHA-256 is `dac0d1dc0720f1b00f84b2208950305f54f94ba5e006dbc43fa77c3512c803e5`. This is runtime/storage software and artifact proof only. PR #153's successor UI code, pinned official-client RF, exact-candidate reboot/reset/recovery and physical qualification, `channel_acceptance_<sha>.json`, dependency closure, WP-09 closure, and release closure remain outside this slice; `proof_banked` therefore remains false.
- PR #115 moves Packet filter/search/pause/paging/fallback and row status/color state into one bounded controller, with separate PSRAM query scratch and an adapter that copies results. The controller has no direct LVGL, UI-task, storage, or RP2040 side effects, but the broader screen/shared-state extraction, lifecycle/generation/redraw invariants, and exact-candidate Packet UI acceptance are unfinished. WP-14 remains blocked and `proof_banked=false`; the portable aggregate SHA-256 is `84ed2f4cf683252ed3cbc8b20c2db9d3de1dace4772818dadc1cbe1b813b655e`.
- PR #121 moves Wi-Fi/BLE build, transport, enabled, and phase truth into a pure bounded connectivity view model. Phase 1 omits controls that cannot work, preserves an enabled-but-unavailable state visibly, and adds no transport/storage/RF/RP2040 side effect. It does not implement BLE, close Wi-Fi reconnect/safe-mode behavior, finish UI ownership, or replace exact-candidate physical UI proof. WP-14 remains blocked and `proof_banked=false`; portable aggregate SHA-256 is `1b2d113353cca9859159349889ea1754706de848f4b5358b2e07320362a270ab`.

- ESP-IDF v5.1.x is end of life and is no longer the selected production target. Standalone migration commit `39a043c` uses version-pinned `espressif/idf:v5.5.4`, commits the exact Actions-generated `dependencies.lock`, applies the tracked Seeed BSP compatibility patch, and passes host, firmware, package, checksum, release, and effective-config checks. That evidence does not yet qualify the combined Map/Wi-Fi candidate: exact COM12 must still report `"idf":"v5.5.4"` and pass board/display/touch/Wi-Fi/RF/RP2040-SD/Map/health/reboot/power-cycle smoke with refreshed commit-matched release evidence. No supported-SDK waiver or hardware qualification is implied by the standalone green build.
- Manual visual confirmation of display bars and touch target movement is still pending; the serial `display test` and `touch test` commands return OK on hardware. The COM12 hardware pixel-capture path reconstructed the current PR #56 hierarchy from `51258ba` / Actions `29060900359` with matching firmware/host CRC `E72745BA`, a passing simulator diff, `public_rf_tx=false`, and `formats_sd=false`; a three-round all-tab health probe also passed cleanly. PR #51, PR #40, and PR #33 captures remain history for superseded layouts. Physical photos/manual review and final-release-SHA evidence are still pending.
- MeshCore Public group text TX/RX and signed advert TX/RX are implemented and validated through the serial console, firmware-local contacts can promote heard nodes by fingerprint, full advertised public keys are retained for DM targeting and have passed reboot persistence on `COM7`, contact favorite/mute flags persist across reboot, Public unread/read state persists across reboot, per-thread DM read cursors are implemented, firmware-local routes are learned from Public/advert path metadata, route detail diagnostics and a first route detail sheet are implemented, and serial DM flood TX plus the bounded DM store are validated. DM ACK/PATH receive parsing, direct-route TX selection, contact-row DM compose, first contact detail actions, a bounded Public History/search sheet, and a bounded scrollable DM thread/detail history sheet are implemented, and targeted outbound hardware DM receive by the local COM11 Meshcorebot has passed. `scripts/rf_full_acceptance_d1l.py` now provides a single COM12-side artifact path for identity, Meshcorebot status, outbound DM, controlled inbound DM, ACK/PATH, direct-route, health, and no-Public-command proof. All current automated RF must use only `#test` or targeted DMs; default Public stays silent. The COM11 Discord Python bot is an independent radio peer and COM11 must never be opened as D1L serial. Manual touch review, a passing full RF acceptance artifact, controlled inbound DM unread proof, and physical DM workflow review are still pending.
- The inbound-DM ACK durability slice uses DM schema v5 for a retained 32-byte authenticated identity digest and per-row ACK reservation/state/error metadata. Valid v5 rows can be deduplicated and bounded across reboot; interrupted reservations are recovered without boot-time RF. Rows migrated from schemas v1-v4 are preserved exactly but marked `legacy_unverified`, are never hydrated into the ACK cache, and are never used as collision-safe re-ACK evidence. A later retry of a grandfathered row may therefore become one new v5 row; firmware does not silently delete or pretend to deduplicate legacy history. Exact peer/RF and power-loss qualification of the v5 behavior remains pending.
- PR #92 expands issue #65's bounded foundation to a 931-case pinned-upstream
  oracle for packet, advert, group, DM, ACK, PATH, TRACE, identity, and login
  boundaries, while retaining the separate 100,000-input wire-envelope fuzz
  gate. Receipts are bound to the exact ordered 22-command build plan and keep
  `closure_ready=false`. Real ACK delivery/correlation, route forwarding,
  generic contact/multi-hop TRACE, admin sessions, retained-state recovery,
  official-peer RF, and hardware remain outside this foundation. PR #93 is now merged and proves
  the first real signed-advert Identity/Mesh/Dispatcher/BaseChatMesh runtime
  path with duplicate, bad-signature, and self-advert suppression plus balanced
  packet allocation/release. It does not claim ACK, route, TRACE, or admin
  closure. PR #96 now makes the production/oracle/runtime arithmetic boundary
  exception-free. PR #101 adds one bounded explicit-loop TRACE request and
  correlated terminal-status path; it deliberately reports
  `contact_trace_supported=false` and `hardware_verified=false`, so the broader
  protocol closure remains unchanged. PR #103 adds fail-closed persisted identity
  classification and exact-full-key verified-contact storage primitives. PR #104
  binds fail-closed identity startup into the production service, and corrected
  PR #105 binds verified signed-advert RX to exact-full-key retained-contact
  promotion with transient-write retry and prefix-collision preservation. PR #112
  adds strict MeshCore URI import/export, exact-key provenance, collision/capacity
  rejection, canonical DM authorization, and injection-safe USB JSON. BLE and
  remaining on-device QR/share surfaces, official-client bidirectional proof,
  reboot/refresh acceptance, and physical qualification remain open. PR #102
  remains a host-only authenticated-admin transcript; concrete packet-pool admin
  dispatch, RF, hardware, and WP-18 acceptance remain open.
- The signed-advert runtime uses the independently checksum-verified
  `rweather/Crypto` 0.4.0 verifier and executes real
  create/sign/send/filter/verify/dispatch/contact-promotion behavior. It does
  not prove firmware binding, persisted keypair consistency, retained-contact
  recovery, peer/RF interoperability, routing, ACK delivery, trace, admin
  sessions, or hardware. The pinned `BaseChatMesh` callback reports
  `is_new=false` for the tested newly auto-added contact; integration must not
  rely on that flag as a truthful new-contact indicator until upstream behavior
  is reconciled.
- The pinned legacy ref10 `fe.c`, `ge.c`, and `sc.c` still contain negative
  signed left shifts, but they are no longer selected by the D1L production,
  oracle, or signed-runtime build boundary. PR #96 selects the reviewed
  215-expression defined-arithmetic overlay and proves byte-identical
  differential/RFC 8032 behavior under pinned Clang 18 ASan/UBSan with zero
  exceptions. `BLK-WP04-ED25519-SHIFT-UB-20260714` is closed. This does not
  close ACK, route, TRACE, admin, retained-state, RF, or physical behavior.
- Reliable inbound DM ACK remains incomplete as a release claim even though its
  software boundary is merged. PR #98 banks the schema-v5 full 32-byte identity
  digest, per-row durable reservation/state/error, no-RF partial-backend failure,
  zero boot-time RF, direct then flood fallback, full-digest callback correlation
  across SD/NVS resequencing, retained TxDone/timeout outcomes, and a maximum
  two-dispatch budget. Valid v1-v4 rows remain `legacy_unverified` and do not
  hydrate collision-safe ACK identity state. Compatible-peer RF, controlled
  power-loss, reboot, and frozen-candidate hardware evidence remain mandatory.
- PR #99 banks only the learned-route software boundary: a known canonical path
  authenticated during the current boot is eligible while fresh, including a
  known zero-hop path; missing, preboot, stale, and malformed paths fall back to
  flood. ACK planning uses the same selector, and telemetry reports the immutable
  route actually sent instead of rereading mutable contact state. Exact-candidate
  RF must still prove direct and fallback delivery, correlation, and retry behavior.
  PR #101 now merges the bounded explicit-loop TRACE software slice, but it
  deliberately does not derive a route from a contact. PR #104 and corrected PR
  #105 bind production identity startup and verified signed-advert exact-key
  contact promotion respectively; PR #102 remains a host-only admin transcript.
  Complete contact lifecycle/recovery, generic contact and multi-hop TRACE,
  production admin dispatch, RF, and physical closure are not claimed.
- Settings are implemented as an NVS-backed firmware model and have passed reboot persistence smoke on real D1L flash. The persisted radio profile is exposed through `settings get`, serial radio setters, and a touch Radio Settings sheet; the user-facing More page groups tools, connections, storage/maps, device, support, and advanced functions behind disclosure rows instead of a flat settings grid. PR #56 proves the collapsed hierarchy and all-tab navigation on COM12; expanded category touch behavior and the remaining system-page redesigns still need physical touch review.
- First-boot onboarding now persists a setup-complete flag, collects the node name, confirms Canada/USA defaults, keeps Wi-Fi/BLE/observer disabled, and generates/retains the local MeshCore identity. Optional-radio choices in onboarding remain limited until live Wi-Fi/BLE runtime support is enabled.
- `identity status` now generates and reports an NVS-retained Ed25519 local identity fingerprint. This is a minimal firmware identity model, not the final full MeshCore C++ store integration.
- PR #120 replaces the old RAM-fallback timestamp path with a centralized truthful-time foundation: monotonic/wall/certificate/protocol clock state, reserve-before-use `mesh_hi_v2` protocol high water, bounded SNTP wait ownership, and fail-closed handling of ambiguous legacy `mesh_ts`. Exact-main `5e83b17efebad8d2e5e689292b57d87070893f02` / Actions `29373261893` passes 1,024 host and 32 checksum tests and the full conformance/fuzz/package gate; portable aggregate SHA-256 is `b4cde8bd2664c9f531927f170e7f9066da4679ee071f179d1ffc0566ba9c324f`. WP-12 is still incomplete: trusted SNTP adoption must occur before any operating-system clock mutation; forward-jump protection, retained wall recovery, explicit legacy migration, timezone/recovery UI, and exact-candidate device/TLS/reboot/power proof remain open. Protocol TX stays fail-closed when legacy persistence cannot be proven safe; no simulation or source review closes that gate.
- The merged #69 retained-store boundary removes synchronous full-route rewrites and storage reinitialization from hot RX observations. A dedicated worker coalesces writes, controlled reboot force-flushes or cancels, and Public/DM/route/packet stores retain independent SD/NVS retry state. The dedicated `d1l_retained` partition uses two v2 ownership markers, a committed in-partition anchor, and a default-NVS completion sentinel; ambiguous nonblank ownership fails closed without automatic erase. Late/removable SD reconciliation is generation-checked before replace-rename, malformed retained envelopes are rejected, and a valid device-local lineage outranks a foreign removable lineage. Historical artifacts `3aefb9f`, `a2695fb`, and `91be531` preserve the stack-overflow, WDT, and false-`no_card` findings plus their bounded repairs. Exact PR #80 source `092293f2311a24c9899bc9bf343ab014c4ba0411` is merged and banks the narrow WP-01 liveness/removal/reboot/active-storage proof. The remaining #69/#78 work is frozen-candidate card/filesystem/electrical/power-loss/final-soak qualification, broader retained-producer ownership, checksummed settings schemas, factory reset, and truthful time—not another PR #80 host or Actions gate.
- ESP-IDF's `nvs_flash_init_partition` is not a read-only discriminator: initialization may erase or activate a corrupt page. Firmware therefore performs zero retained-region erases and does not call NVS initialization on ambiguous nonblank bytes with neither marker nor sentinel ownership; it reports `external_init_required=true`. A separately audited installer/hardware step must verify the supported predecessor layout before erasing only `d1l_retained`; firmware then verifies blank first use and establishes marker 1, NVS, anchor, marker 2, migration, and sentinel in order. The known failed pre-anchor recovery cannot be authorized by a similar-looking live status: the installer also requires the hash-pinned incident manifest, exact historical receipt hashes, exact table artifact, live ESP32-S3 MAC, the audited prior MeshCore identity, and exact full raw-region SHA, then records durable pre-erase/erase/verify stages without retaining a raw backup. Simultaneous loss of both markers and the default sentinel, including an anchor-only NVS, stays fail-closed and preserved until that external decision; it is not an automatic deletion case.
- NVS initialization is fail-closed and non-destructive: DeskOS no longer erases the whole NVS partition automatically for `ESP_ERR_NVS_NO_FREE_PAGES` or an unsupported stored NVS version. The boot event and `health` expose a per-boot nonce plus `nvs_ready`/`nvs_error`; persisted settings remain untouched for explicit recovery. In this degraded mode NVS-backed features may be unavailable until the underlying capacity/version problem is resolved, and exact hardware recovery UX remains open under #69.
- MeshCore 3-byte companion framing is implemented and host-tested, but the live binary bridge to MeshCore protocol frames is not enabled in Phase 1.
- Packet log storage keeps a 128-row active RAM ring and persists the newest 8 rows through the compact NVS fallback. When the RP2040 SD file protocol is ready, packet history uses a split retained-store write path: SD receives the primary snapshot through coalesced flushes while NVS keeps the newest fallback rows, and a validated 4096-record segmented SD journal backs 100-row Packet tab pages with Load Older/Newer. Packet rows carry a bounded raw hex preview and expose serial filter/search/raw commands plus a touch terminal-style Packet tab with RX/TX/fail/error coloring, pause/resume, filter/search, route evidence, and normal/advanced packet detail; full hardware proof of 24h SD scrollback and deeper protocol decode are still pending.
- Optional SD-card data storage is FAT32-only and non-destructive. Merged PR #61 / source `4d9f384` proved the read-only Storage/Card/Data hierarchy on exact Actions-built COM12 captures with CRCs `9878ADFB`, `77726394`, and `42619B0E`, passing simulator diffs, moved-and-restored scrolling, and a clean three-round probe without Public RF or formatting. Manual physical touch/photos remain open, and this UI proof is not new SD matrix evidence. Current serial commands and the RP2040 bridge remain non-formatting, and users must provide FAT32 cards prepared on a computer. Exact `91be531` hardware evidence isolated the intermittent inserted-card disappearance to the RP2040 cached `no_card` gate: one quiesced controlled reboot passed as `SW`, while the second was safely cancelled after a route SD write saw `ESP_ERR_NOT_FOUND`; the card later reported `READY_SD`, with zero route rename failures, zero other retained-store errors, and no new WDT/PANIC. Exact PR #80 source `092293f2311a24c9899bc9bf343ab014c4ba0411` applies a bounded read-only sector-zero liveness transaction after three suspect GPIO7 samples and subsequently passed the narrow WP-01 inserted-card, 10/10 physical removal/reinsert, 5/5 retained reboot, and 7,207.089-second active-storage proof. That source proof is banked and PR #80 is merged, but it does not close `4ee07caf`, `17a948cf`, or frozen-candidate physical roles, nor the broader no-card, unusable/non-FAT32, multi-card/size, electrical, power-loss, or final-soak matrices. Local-dark tile proof and signed peer-advert node-marker proof also remain pending. D1L has no onboard GPS; Map center is manual and DeskOS makes no local-GPS claim.
- Older guarded-format hardware artifacts from July 1-2, 2026 are now obsolete for release readiness because the public policy forbids on-device formatting. Fresh SD acceptance evidence must use a FAT32 card prepared on a computer, prove auto-creation of the DeskOS directory structure, prove no-card NVS fallback, and prove non-FAT32/unmountable cards remain non-destructive with clear repair guidance.
- Historical scoped COM12/COM16 evidence from `1a29876` / GitHub Actions run `28714355561` proves the core SD path on the installed FAT32 card without Public RF or formatting: the official Seeed SD smoke passes on COM16, COM12 smoke reports `READY_SD`, raw RP2040 diagnostics complete, the file canary passes, correct/missing/existing-data boot scenarios pass, RP2040-unavailable fallback is captured after official smoke, retained Public/DM/route/packet history survives reboot, reboot/remount passes, the synthetic map-tile canary passes, and export canary/diagnostic export/sampled data export all pass. It does not qualify the current or final candidate. Public release is still blocked on the remaining physical SD evidence matrix: actual no-card and unformatted/non-FAT32 scenarios, FAT32-only rejection behavior for unusable media, <=32GB multi-card proof, no-format guidance evidence tied to unusable media, and power/electrical evidence.
- WP-01 is `merged` with `proof_banked=true`. Its accepted source remains `092293f2311a24c9899bc9bf343ab014c4ba0411`, Actions `29272708844` / `29272709642`, and aggregate SHA-256 `994f4e5ac7b9e0e8bdb57aad7715f52a99294a1841847860e2ce2f70bd6e2277`; that predecessor proof does not qualify the successor or final release matrices.
- WP-02 remains `in_progress` only for physical qualification. The runtime integration PRs #62/#64/#80/#84 are merged; the historical release-security checkpoint `e79fb56160914f4483515f4f70998aa2f8961496` passed exact-main run `29300795502` and downloaded checksums, and later WP-03/WP-04 slices have advanced live main. The tracked portable integration baseline `docs/completion/evidence/wp02/integration_baseline_4ee07caf09906abdcebe8faccd95790dceb5fe88.json` (SHA-256 `39d8632d6de5bc819a96e92e970b9d280130a3014336be5d045a1f3fe07b654c`) lacks only five physical role receipts. They will be recaptured on the frozen final candidate, not inferred from this or any predecessor SHA.
- Phase 6 mesh visibility exposes `signal`, `roomservers`, `repeaters`, routes, and Home/Packet summaries. PR #60 / source `0b138be` proved the full-height read-only Mesh Roles hierarchy on exact Actions-built COM12 captures with CRCs `63DE54FB`, `FD538D71`, and `5C41EE08`, passing diffs and a clean three-round probe without Public RF or formatting. Manual physical Back/scroll review remains pending. Authenticated compatible repeater/room login and management are now required for 1.0 under #77 but are not implemented or release-proven yet; current role pages remain read-only.
- Contact export emits MeshCore-compatible `meshcore://contact/add?...` payloads over serial with LVGL QR rendering enabled in the default build and requires a promoted contact with a retained 64-hex public key plus a known canonical role. Canonical chat contacts place Message and Contact options at the root; non-chat and unknown roles do not expose a dead Message action, and unknown/malformed roles show Export unavailable. Route, valid Export, Rename, favorite/mute state, and confirmed removal live under Contact Options. Merged PR #59 / `d24552e` / Actions `29064260772` proved the earlier Contact Detail, Contact Options, and confirmation-only Forget pages on COM12 with exact CRC matches and passing simulator diffs, without invoking deletion, Public RF, or SD formatting. The new role gates, physical touch review, and scan/import proof with a MeshCore client remain pending.
- The touch shell implements Home/Messages/Network/Map/More plus full-height nested messaging, contact, Mesh Roles, and Storage pages. PR #56 proves Home, PR #58 proves DM Thread, PR #59 proves contacts, PR #60 proves Mesh Roles, PR #61 proves Storage, and PR #35 proves the retained keyboard callers. The revised built-in-source Map starts at zoom 10, permits zooms 8 through 14, and exposes one-finger pan plus direct `-`, `+`, and `Center` controls while retaining `Map -> Map options -> Set location or Cache status`. Pan/zoom are session view state rather than saved preferences. A physically opened Map can wait on cached SD readiness and resume the same visible generation, while the storage manager permits only one bounded pre-response bridge reset per ESP32 boot after three initial timeouts. These recovery paths still need exact Actions-built power-cycle proof. Background fetch, multi-zoom prefetch, off-screen arbitrary-coordinate batches, and area download are intentionally unsupported.
- `tools/ui_simulator.py` provides deterministic 480x480 host screenshots, schema-v2 required-label/text-overflow checks, 44x44 touch-target checks, and expected flows. Map scenarios cover no location, Wi-Fi off, Wi-Fi connecting, live current-view, and cached revisit states; they record the zoom-10 default, 8-through-14 range, one-finger pan, direct controls, a 9-tile visible limit, one zoom per visible generation, completed same-view frame reuse, no background/prefetch/area download, and visible `(c) OpenStreetMap contributors`. This is a fast CI regression check, not a replacement for physical control/tile/render/network/memory proof.
- A UI refresh fix has been added for the reported split-page/live-packet corruption: RF-mutated packet/message/DM/node/route/contact stores and mesh-inspector scratch buffers serialize reads and writes before UI snapshots, touch callbacks request coalesced content refreshes, and live data generation changes queue redraws for data-dependent tabs. Synthetic Public, DM, route, and packet UI canaries now use separate preview slots: they remain visible to the UI but cannot consume durable sequence/count/drop metadata, evict a retained row, or enter an NVS/SD flush; the next real row supersedes the preview. The revised Map acknowledges unrelated live counters and transient SD busy/readiness changes without tearing down its viewport, drag, or unfinished tile batch; only changed saved-location/default-zoom inputs request its full redraw. If SD is still preparing, the already-authorized visible Map generation waits on cached status and resumes without a page rebuild. `59610ab` / Actions `28723265336` passes the earlier longer targeted corruption probe, PR #56 / Actions `29060900359` passes the current hierarchy Home capture plus three-round all-tab health proof, and PR #35 / Actions `28727064923` passes the all-target keyboard capture. The revised canary isolation and Map guard still need commit-matched Actions and COM12 proof. Final release also needs physical photos/manual review, full RF acceptance, remaining SD matrix evidence, remaining #16/#6 UI ownership evidence, and the 12-hour soak.
- Messaging, contact, packet, radio, hierarchical Mesh Roles, and Storage pages have host coverage; PR #58 proves DM Thread, PR #59 proves contacts, PR #60 proves Mesh Roles, and PR #61 proves Storage on COM12. The current Map hierarchy has standalone Actions coverage, but its combined-candidate pixels and physical live-flow proof remain open; manual physical review remains open across these surfaces.
- The SX1262 Phase 1 probe intentionally avoids TCXO/DIO3 control. TCXO remains `NONE` by default.
- Wi-Fi and BLE are managed by an offline-first companion-radio policy. Wi-Fi remains off by default, but the Phase D station path can be explicitly enabled for bounded `wifi scan`, saved-profile `wifi connect`, and touch Wi-Fi Setup use without printing saved passwords. BLE companion transport remains build-disabled/pending. Standalone `de79c9f` passes exact-Actions-artifact Wi-Fi hardware proof; the full enable/connect/reconnect/reboot/low-memory path must be repeated on the combined ESP-IDF v5.5.4 artifact.
- Phase 7 diagnostics now expose a small reset/crash ring and richer health telemetry. A repeatable soak runner samples health, mesh, signal, unread, packet, command retries, and crashlog state. Historical Public `test` soaks on `COM7` remain regression history, but current automation uses only `#test` or targeted DMs and never default Public. A qualifying soak artifact must include its exact command manifest; the full 12-hour idle/listening soak is still pending.
- `scripts/release_gate_audit_d1l.py` provides a machine-readable, commit-metadata-strict release audit. PRs #86-#90 cover immutable build inputs, exact package inventory, deterministic SBOM, unsigned provenance, and reproducibility. WP-03 is banked by exact-source comparator receipt `babb5d8c42133ab2e0d42fc38633fbba9976c17cfd42de1eb05b5559253ac11f` and tracked aggregate `docs/completion/evidence/wp03/release_reproducibility_e79fb56160914f4483515f4f70998aa2f8961496.json` (SHA-256 `ff97327ae7a6c7e90f2db8905ffe344dbe73d0fb75065bbf6f66294b5c72e264`). The older `invalid_sbom` receipt remains preserved as fixed negative history. These host/CI gates do not replace issue #63 COM12 `version.idf`, behavioral, or final evidence requirements. The current audit remains fail-closed; issue-sized artifacts, including the banked WP-01 receipt, do not replace the final frozen release commit's matching RF/DM, broader SD/card/power, 12-hour soak, boot, Seeed, manual physical UI/photo, architecture, and final sweep evidence.
