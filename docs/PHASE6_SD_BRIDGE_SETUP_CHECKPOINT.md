# Phase 6 SD Bridge Setup Checkpoint

This checkpoint advances optional SD-card data storage without making boot or retained data depend on the card.

## Implemented

- Added an ESP32-side RP2040 SD status probe using the line protocol `DESKOS_SD_STATUS`.
- Superseded 2026-07-02: the guarded ESP32/RP2040 SD format path has been removed. Users must provide FAT32 cards prepared on a computer.
- Extended `storage status` with RP2040 protocol, card, filesystem, DeskOS root, capacity/free, setup, and `needs_fat32` fields.
- Added non-destructive `storage setup`.
- Kept `storage setup` non-destructive and policy-only.
- Added a Settings-tab Storage Setup sheet and simulator coverage.
- Added `tools/rp2040_sd_protocol.py` as the host reference simulator for the RP2040 line protocol.
- Added `firmware/rp2040_sd_bridge/deskos_sd_bridge` as an original Arduino RP2040 SD bridge target for the D1L internal UART and SD pins.
- Added a GitHub Actions-only RP2040 bridge build job that compiles with Arduino CLI and uploads checksummed `rp2040-sd-bridge-firmware` artifacts.
- Added bounded generic `DESKOS_SD_FILE v=1` file operations to the simulator, RP2040 bridge target, and ESP32 bridge API: `stat`, `read`, `write`, `append`, `delete`, and `rename` with sanitized relative paths, base64url payloads, CRC32 checks, 512-byte lines, and 192-byte decoded chunks.
- Added an SD-capable retained blob-store provider for the packet-log canary. It enables SD only when the RP2040 reports ready data, file operations, matching line/path/chunk limits, and atomic rename; writes use `stores/packet_log/ring.tmp` plus `rename replace=1` to commit `stores/packet_log/ring.bin`.
- Kept an onboard NVS mirror for the packet-log canary and retained NVS fallback on no-card, timeout, missing file support, missing atomic rename, insufficient limits, and invalid SD blob cases.
- Added serial-only diagnostic export, sampled data export, and map-tile cache canaries. Diagnostic exports commit under `exports/diagnostics/`, sampled data exports commit under `exports/data/`, and the map-tile cache canary commits a synthetic tile under `map/tiles/`. They use temp write/read plus `rename replace=1`, leave the final artifact present for inspection, and do not send Public RF or format.
- Kept settings, identity, contacts, read-state, crashlog, and the full map page/tile download policy on onboard/fallback storage or pending; no card-dependent boot state is claimed in this slice.
- Matched the Seeed D1L ESP32/RP2040 UART pin contract at ESP32 UART2 GPIO19/GPIO20 and uses 115200 baud for reliable bounded command exchange, enabled the RP2040 SD/sensor rail on GPIO18, added safe/cached `DESKOS_SD_STATUS`, explicit `DESKOS_SD_MOUNT`, and raw SdFat diagnostics.
- Follow-up pending hardware proof: the RP2040 bridge now has `DESKOS_SD_PING`, safe `DESKOS_SD_STATUS`, explicit async `DESKOS_SD_MOUNT`, async-safe `DESKOS_SD_DIAG`, and a bounded raw SPI presence probe before Arduino/SdFat mount attempts; ESP32 exposes `rp2040 ping`, `storage status`, `storage mount`, and `storage diag`. This is intended to keep boot/UI polling non-blocking while giving the operator a deliberate mount path for physically inserted cards.

## Validation Rules

- Do not run firmware builds on the Windows host. Firmware artifacts must come from GitHub Actions.
- Local verification for this slice uses downloaded GitHub Actions artifacts plus serial hardware proof. Firmware builds and CI-style checks run in GitHub Actions, not on the Windows host.
- Do not test RF on Public channel for this slice. Use only the operator-confirmed D1L validation port for the current test path; in the latest direct RP2040 checks this was COM16. Do not open forbidden ports during this SD bridge slice.
- After the RP2040 bridge is flashed, run preflight first so it captures `rp2040 ping`, safe `storage status`, explicit `storage mount`, optional `storage diag`, and `health`. The preflight may poll safe status if a build reports `state="mount_pending"`, but the current filesystem mount path is constrained to the Arduino-Pico documented SPI1 setup, registering GPIO13 as SPI1 CS, idling CS high, enabling the MISO pull-up/input path before and after SPI1 starts, floating SD SPI pins during forced GPIO18 rail-off so CS/MOSI/SCK cannot backfeed the card, waiting for rail settle, and calling Seeed's `SD.begin(13, 1000000, SPI1)` sample shape without running a second SdFat probe on the first failure. Use `storage filecanary` or `python .\scripts\sd_file_canary_d1l.py --port <operator-confirmed-d1l-console-port>` only once the ready file gate is available, then `python .\scripts\sd_map_tile_canary_d1l.py --port <operator-confirmed-d1l-console-port> --token map1` for the map-tile cache proof. These canaries are serial-only and do not send Public RF or format SD.

## Hardware Evidence

- GitHub Actions run `28490556715` for commit `1afd4043ce2473dddeaa66f23fafb667d2a98b88` passed host checks, ESP32 firmware build, and RP2040 SD bridge build. Its checksum manifests were verified locally before hardware use.
- The D1L ESP32 was flashed on COM12 only from that Actions release package. COM12 smoke passed after flashing.
- The RP2040 UF2 volume later mounted as `RPI-RP2` at `G:\`, and Actions artifact `28494746866` was copied with the guarded helper. Copy evidence is `artifacts/rp2040-flash/rp2040-sd-bridge-uf2-copy-28494746866.json`; its UF2 SHA-256 was `05A2D728EC5EC59875C67EC2EC926B31F6032DAB1CBCD88DCA19AF54E655CC94`.
- Post-copy COM12 proof in `artifacts/hardware/com12/rp2040_sd_preflight_after_uf2_copy_28494746866.json` failed safely with `state="rp2040_protocol_pending"`: ESP32 UART2 to RP2040 was ready, but `storage status` and `storage diag` timed out. No Public RF or formatting occurred.
- The likely bridge-side cause was fixed by commit `e05264098a106f1ba0adcf766a1262d12e73448c`: the RP2040 status path now runs raw SdFat probes before any FAT mount attempt and can answer the same line protocol on USB serial for debugging.
- GitHub Actions run `28495545520` passed for that commit. The current verified RP2040 UF2 SHA-256 is `AFB6B12EE3518C48811F6C2876717B9BFAF43C1ABFE02E9BF693D95F977E16E5`.
- Actions run `28499319258` for commit `a8268073ae290567e26f27491c8ffa167f6f8d57` flashed the ESP32 on COM12 and copied the RP2040 UF2 with SHA-256 `AA71CD32C9433F1D57B1C3F243ABB2A7535728E6A3905C6A424A1E77D5F3E57E`.
- COM12 evidence `artifacts/hardware/com12/rp2040_sd_preflight_ping_status_28499319258.json` proves `rp2040 ping` works with `protocol_supported=true`, `sd_touched=false`, file limits, and `atomic_rename=true`; the same run still timed out on SD-touching `storage status` and `storage diag`.
- Actions run `28500532119` for commit `cfb99620ee691097d845fb0e1920c06bbd02d1a5` passed host checks, ESP32 firmware build, and RP2040 SD bridge build. The release package and RP2040 checksum manifests verified locally, then the ESP32 was flashed on COM12 and the RP2040 UF2 with SHA-256 `44F2D152818C3EDDEA60165B268234742D2EAD1281F345F7B059ADBB472E3B9C` was copied through the guarded helper.
- COM12 evidence `artifacts/hardware/com12/rp2040_sd_preflight_mount_split_28500532119.json` proves `rp2040 ping` works, initial safe `storage status` returns `state="mount_required"` with NVS fallback, and no Public RF or formatting occurred. The same run showed explicit `storage mount` still timed out and made later SD-touching commands unresponsive, so the follow-up implementation moves mount/diag work to the RP2040 core1 worker and reports `state="mount_pending"` while probing.
- Actions run `28501529179` for commit `0697d61cac2fcf7bb67a54312ec591e91b0ce363` passed host checks, ESP32 firmware build, and RP2040 SD bridge build. COM12 evidence `artifacts/hardware/com12/rp2040_sd_preflight_async_mount_28501529179.json` proves `storage mount` now returns `state="mount_pending"` without formatting or Public RF, but the later Arduino/SdFat mount path still stalled RP2040 status replies; the current follow-up adds a bounded raw SPI presence probe before any filesystem mount attempt.
- Actions run `28502095977` for commit `552a59df3a5810c5004f496584020d31f5f1dea4` passed host checks, ESP32 firmware build, and RP2040 SD bridge build. COM12 evidence proved the bounded raw probe kept the bridge responsive and detected the inserted card without Public RF or formatting; the file-operation gate was still not ready.
- Actions run `28502609315` for commit `b8d374246310ac761cbd965ae59a0bd0e1b6883f` is retained as historical evidence only. Its guarded-format path has been superseded by the FAT32-only no-format policy.

## Remaining SD Work

- Current follow-up: the direct high/dedicated SdFat mount did not produce a ready FAT32 filesystem even though the user-confirmed FAT32 card was inserted. Actions run `28604061282` for commit `4064823b72d96e1e39bc1252b50fa7e39e079505` passed and was flashed locally; COM12 evidence `artifacts/hardware/com12/rp2040_preflight_4064823_actions_28604061282_COM12.json` proved `rp2040 ping` before SD touch but the Arduino `SD`/`SDFS` filesystem path wedged later status/diag replies during `storage mount`. Actions run `28604864713` for commit `3f7e2ab3977218758b9552ac912a1ac9d763d972` fixed the wedge: `artifacts/hardware/com12/rp2040_preflight_3f7e2ab_actions_28604864713_COM12.json` keeps ping/status/diag responsive and reports `sd_probe_rejected_card` with `probe_error=3` / `probe_data=0`. Actions run `28605782601` for commit `4d691b36a19874fb10cc22b6f7e53ee7a2fe03ac` kept the bridge responsive on direct COM16 USB but still rejected the inserted FAT32 card with `probe_err=4` / `probe_data=0` in `artifacts/hardware/com16/rp2040_direct_probe_4d691b3_actions_28605782601_COM16.json`. Actions run `28606586139` for commit `151ab6fda4cd821fc195cdfa8d85ebfda97d9a6c` proved the already-powered Seeed-style `SD.begin(13, 1000000, SPI1)` path wedges when it runs from the RP2040 core1 worker. Actions run `28607365698` for commit `1303ebfda40af4b7872675811cddffe0f8600316` moved the explicit mount to the protocol-handling core, flashed RP2040 and ESP32 from the downloaded artifact, and still timed out on COM12 with `state=sd_mount_pending` / `sd_last_error=ESP_ERR_TIMEOUT` in `artifacts/hardware/com12/rp2040_preflight_1303ebf_actions_28607365698_COM12.json`. Actions run `28608112968` for commit `d29e64885131995ae062abb40f867e4506be591e` removed explicit SPI CS registration and extra SdFat probing; the bridge stayed responsive after flashing both chips, but `storage mount` still returned `state=no_card` with `probe_error=4`, then later `storage status` was overwritten by a stale pending snapshot. Actions run `28608728222` for commit `a2be32da00811689ca26041e818d395f91b06d42` fixed the stale status overwrite and kept the bridge responsive, but still reported final `state=no_card` / `probe_error=4` in `artifacts/hardware/com12/rp2040_preflight_a2be32d_actions_28608728222_COM12.json`. Actions run `28609225249` for commit `c4f6c28948eb0f632a3d461fcff2194556922281` removed custom SdFat/SPI compile overrides and kept Arduino-Pico default SD library settings, but the flashed device still reported `state=no_card` / `probe_error=4` on the user-confirmed FAT32 card. Actions run `28609861698` for commit `ce902153cd4301c77fcddd146e6ee6dc7841d5ea` registered `SPI1.setCS(13)` and used `SD.begin(13, SPI1)`; direct COM16 RP2040 evidence `artifacts/hardware/com16/rp2040_direct_preflight_ce90215_actions_28609861698_COM16_retry.json` kept the bridge responsive with `formats_sd=false`, but still returned `state=no_card`, `probe_err=4`, and all diag candidates `*_e=4`. Actions run `28610808704` for commit `6b66452be30d2d69b0a2d539288e3c5b7c1e63ca` added raw command diagnostics; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_diag_6b66452_actions_28610808704_COM16.json` shows all candidates answering `CMD0=0`, `CMD8=0`, and R7 `[0,0,0,0]`, confirming an all-zero SPI response path. The next bridge artifact enables the RP2040 internal pull-up on SD MISO before and after SPI1 claims the pin. Use `mount_error`/`mount_data`, `probe_error`/`probe_data`, and the raw diagnostic fields as firmware-side diagnostics; do not treat reformatting the card as the fix.
- Actions run `28611823082` for commit `a411569a14773a4157f211dbc8772d58c1ce788f` kept the bridge responsive after enabling the RP2040 MISO pull-up before and after SPI1 starts, but direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_a411569_actions_28611823082_COM16.json` still reports `state=no_card`, `probe_err=4`, `CMD0=0`, `CMD8=0`, and R7 `[0,0,0,0]` across all candidates. Actions run `28612512920` for commit `5797cf244b1ae07e4e3fe6267233d1bfe85a0bb9` proved the GPIO12 input-buffer build passes Actions and direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_5797cf2_actions_28612512920_COM16.json` shows GPIO12 samples high (`*_miso_pull=1`, `*_miso_spi=1`, `*_miso_idle=1`) while SPI command responses remain all-zero. Actions run `28613020379` for commit `998e835205fffdcd4617afb6dac42eb678ca1eac` proved the SPI1 reset/pin-acceptance build passes Actions; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_998e835_actions_28613020379_COM16.json` shows all SPI1 pins accepted and CS-high idle reads `255`, but CMD0/CMD8 responses still return zero. Actions run `28618188991` for commit `b066f4ed2ec98f8958ac2add4e623b7b6c27c664` proved the software-CS build passes Actions; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_b066f4e_actions_28618188991_COM16.json` still reports `state=no_card`, `probe_err=4`, and all-zero CMD0/CMD8/R7 responses. Actions run `28618724956` for commit `b8f3f73e6a16e2ab73ccc035dbaeb1a9e4cbc678` proved the busy-low parser build passes Actions; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_b8f3f73_actions_28618724956_COM16.json` shows `_c0r=0` and `_c8r=0`, so the card-response line is held low while selected before the command bytes. Actions run `28619164143` for commit `e189e586f4f0dc7416e8e98aad53f93c8d9e903b` proved the longer SD rail off/on settle and selected-card ready wait build passes Actions; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_e189e58_actions_28619164143_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_poll_e189e58_actions_28619164143_COM16.json` still reports `state=no_card`, `probe_err=4`, accepted SPI pins, CS-high idle `255`, idle MISO high, and selected high/dedicated command bytes stuck low (`hd_c0r=0`, `hd_c8r=0`, `hd_c0=0`, `hd_c8=0`). Actions run `28619773565` for commit `0e2d83c48736b5f0dc2ebd59aef90928abb9eba3` proved the Seeed-style `SD.begin(13, 1000000, SPI1)` build passes Actions; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_0e2d83c_actions_28619773565_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_poll_0e2d83c_actions_28619773565_COM16.json` still reports the same firmware-side all-zero selected-card response path. Actions run `28620481040` for commit `b7777a56fa5f19bea0cd13c409660ec6421011f4` proved the pre-power CS-high/MOSI-high/SCK-low line-bias build passes Actions; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_b7777a5_actions_28620481040_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_poll_b7777a5_actions_28620481040_COM16.json` still reports the same selected-card low response path. The validation card is user-confirmed FAT32, so this remains a firmware-side SPI/power/select initialization blocker rather than a filesystem-format blocker.
- Actions run `28622219945` for commit `8c9e5f9c4566ac93c52ed218d6805b31c97c4039` proved the GPIO7 SD-detect/probe-rejection build passes Actions; direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_8c9e5f9_actions_28622219945_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_poll_8c9e5f9_actions_28622219945_COM16.json` keeps the bridge responsive, reports `detect=low`, `detect_driven=1`, `det_pullup=0`, `det_pulldown=0`, and now classifies the all-zero selected-card path as `state=error note=sd_probe_rejected_card` instead of ordinary `no_card`. The completed high/dedicated diagnostic still has accepted SPI pins, CS-high idle `255`, idle MISO high, and selected CMD0/CMD8/R7 bytes stuck at `0` (`hd_c0r=0`, `hd_c8r=0`, `hd_c0=0`, `hd_c8=0`, R7 `[0,0,0,0]`). This remains a firmware-side RP2040 SPI/power/select blocker against the user-confirmed FAT32 card; no SD formatting or Public RF occurred, and COM11/COM29 were not opened.
- Actions run `28623802315` for commit `e91c6e76f107c60bb35bfbe755e3b7532563d268` proved the stricter CMD0 reset-entry build passes Actions and was flashed from downloaded UF2 SHA-256 `1410C120D5B153B4568826F334800F31476BDD101D2B864EF2D883875509B1AD`. Direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_e91c6e7_actions_28623802315_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_poll_e91c6e7_actions_28623802315_COM16.json` keeps the bridge responsive, reports `detect=low`, `detect_driven=1`, `probe_err=3`, `probe_data=0`, `hd_c0r=255`, `hd_c0=0`, and no CMD8 attempt after CMD0 failed to enter idle. This narrows the firmware-side blocker to SD SPI reset-entry/power/select behavior before FAT32 filesystem parsing.
- Actions run `28635381685` for commit `52e4cd95827a99699c4a3a4409c674d7f74be973` proved the Arduino-Pico SPI1 pin-method build passes Actions and was flashed from downloaded UF2 SHA-256 `35A15E0A212D14A5FE3D695B8F04A42F819AC05B1F6913FDAF5224E41392080D`. Direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_52e4cd9_actions_28635381685_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_poll_52e4cd9_actions_28635381685_COM16.json` still reports `state=error note=sd_probe_rejected_card`, `probe_err=3`, `probe_data=0`, `detect=low`, `detect_driven=1`, accepted SPI pins including CS, and high/dedicated `hd_c0=0`; COM11 and COM29 were not opened. The next diagnostic should bypass SPI1 with a bit-banged raw probe before choosing a filesystem fallback.
- Actions run `28635801490` for commit `fcfb0f7a4ab159d2c19b3a2ef51933cde6c20cf2` proved the bit-banged SD diagnostic build passes Actions and was flashed from downloaded UF2 SHA-256 `069B39D65AE325281D063D982BCB7BD91AF9EEF7EF95CC5229653BA6C43402F5`. Direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_fcfb0f7_actions_28635801490_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_poll_late_fcfb0f7_actions_28635801490_COM16.json` keeps the bridge responsive, reports `detect=low`, `detect_driven=1`, high/dedicated `hd_e=3` / `hd_c0=0`, and bit-banged `bb_e=3` / `bb_c0=0`. This rules out the SPI1 peripheral as the sole cause and moves the next firmware change to true rail-off reset sequencing: float SD SPI pins while GPIO18 is off, then re-bias and settle before Seeed/Arduino-Pico init. COM11 and COM29 were not opened.
- Actions run `28636238157` for commit `ac8cc1a5e4f7837ca13e93970a22044693f8705f` proved the no-backfeed rail reset build passes Actions and was flashed from downloaded UF2 SHA-256 `20C3BA2261AF070C66EE4D75F76FC6FAA4CDB0B06F1B92D4E6E06DE4AF2C0B89`. Direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_preflight_ac8cc1a_actions_28636238157_COM16.json` and first-touch diag evidence `artifacts/hardware/com16/rp2040_direct_diag_first_ac8cc1a_actions_28636238157_COM16.json` still report `detect=low`, `detect_driven=1`, high/dedicated `hd_e=3` / `hd_c0=0`, and bit-banged `bb_e=3` / `bb_c0=0`. This proves the Seeed `SD.begin` attempt is not poisoning the card first and the next diagnostic is bit-banged CMD0 byte-boundary recovery before changing the filesystem path. COM11 and COM29 were not opened.
- Actions run `28636546867` for commit `f5b786291dc7f87275f68919ad75fba481f7d7c4` proved the bit-slip diagnostic build passes the RP2040 Actions job and was flashed from downloaded UF2 SHA-256 `C3598A22534B2F23067A422087FAC0210811BBBD55DC6CC3658974855AA19548`. Direct COM16 first-touch and late diag evidence `artifacts/hardware/com16/rp2040_direct_diag_first_f5b7862_actions_28636546867_COM16.json` and `artifacts/hardware/com16/rp2040_direct_diag_late_f5b7862_actions_28636546867_COM16.json` still report `detect=low`, `detect_driven=1`, high/dedicated `hd_e=3` / `hd_c0=0`, and bit-banged `bb_e=3` / `bb_c0=0`. This rules out byte-boundary recovery and moves the next firmware diagnostic to scanning past leading zero response bytes inside the CMD0 response window before changing FAT32/file handling. COM11 and COM29 were not opened.
- Actions run `28636906238` for commit `48c6f6f9987968ff35575697e8fa9a10deeeca0d` passed RP2040, host-check, and D1L firmware Actions jobs and was flashed from downloaded UF2 SHA-256 `9F9D9A676F922E69C34F1841C366F672F56E4A8E6F3A8E7E9CD1F32DE90D39FB`. Direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_diag_first_48c6f6f_actions_28636906238_COM16.json` still reports `detect=low`, `detect_driven=1`, high/dedicated `hd_e=3` / `hd_c0=0`, and bit-banged `bb_e=3` / `bb_c0=0` after scanning leading zero response bytes. The next firmware change follows the SD SPI command rule that the card is ready for a command only when DO/MISO is high after CS is asserted, so CMD0 will wait for selected-ready before reset-entry. COM11 and COM29 were not opened.
- Actions run `28637131794` for commit `df43d04570d8d9fc5396a338ebb5286ef3ff4bb1` passed RP2040 and host-check Actions jobs and was flashed from downloaded UF2 SHA-256 `E587DEFD47F4636B8638D47625B4588A4C96101A9B8010997710A7903BBA08CD`. Direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_diag_late_df43d04_actions_28637131794_COM16.json` reports `detect=low`, `detect_driven=1`, high/dedicated `hd_c0r=0` / `hd_c0=0`, and bit-banged `bb_c0r=0` / `bb_c0=0`, proving the card holds DO/MISO low when CS is asserted before CMD0. The next change keeps that evidence but bounds the selected-ready sample so diagnostics do not stall on the all-zero card-response path. COM11 and COM29 were not opened.
- Actions runs `28637467289`, `28637622780`, `28637904067`, `28638307188`, and `28638546447` passed the RP2040/host/D1L firmware Actions jobs while extending the non-formatting diagnostic matrix. The latest bridge UF2 SHA-256 is `040D90497EE2300B48E82A1F23AC382F83639A1743EDC513A2824CA13ABF42C8`; it was restored to COM16 after the smoke test and `artifacts/hardware/com16/rp2040_bridge_ping_restored_205f4de_actions_28638546447_COM16.json` proves `DESKOS_SD_PING` responds with `sd_touch=0`. Direct COM16 evidence `artifacts/hardware/com16/rp2040_direct_diag_late_b79b713_actions_28638307188_COM16.json` rules out inverted CS and safe pairwise SCK/MOSI, CS/MOSI, and SCK/CS input-line swaps: normal/high, bit-banged, SCK/MOSI-swapped, and SCK/CS-swapped paths still report selected CMD0 low (`*_c0r=0`, `*_c0=0`), while CS/MOSI-swapped reports no card response (`bcm_c0=255`). `artifacts/hardware/com16/rp2040_direct_mount_b79b713_actions_28638307188_COM16.json` still reports `state=error note=sd_probe_rejected_card probe_err=3 probe_data=0`. Commit `f13ba46ea21d42d3c8edfa2e642d7c57d54391a0` changed the bridge to Seeed's exact `SPI1.setTX(11)` / `SPI1.setRX(12)` calls; Actions produced the same UF2 SHA as the alias build, confirming no binary behavior change. Commit `205f4de65bc8a5ac10977505201cf920bfb32ac0` added an Actions-built `rp2040-sd-smoke-firmware` artifact with the minimal Seeed MicroSD init shape. That smoke UF2 SHA-256 was `9166BC589F1C2374718A65490DF038CFBFC42BA7FA092E34D96C1AC10AC72496`; after flashing it on COM16, `artifacts/hardware/com16/rp2040_seeed_sd_smoke_205f4de_actions_28638546447_COM16.json` reported `SEEED_SD_SMOKE sd ok=0`. This proves the SD failure is below the bridge protocol/filesystem layer and still before FAT32 parsing; the card is user-confirmed FAT32, no SD formatting command was sent, no Public RF was sent, and COM11/COM29 were not opened.
- Actions run `28639072595` for commit `4b9029116149899550e4969091bb433c292b49cb` tested a researched SPI mode-3 bit-banged CMD0 diagnostic from a downloaded UF2 SHA-256 `9D688D7EF4A84115808449E81BCD3CD5B4084F363C2C9B0CDACBE6D2182EDB85`. The first COM16 request returned only the pending diagnostic shape in `artifacts/hardware/com16/rp2040_direct_mode3_diag_4b90291_actions_28639072595_COM16.json`, and follow-up `DESKOS_SD_DIAG`, `DESKOS_SD_MOUNT`, and `DESKOS_SD_PING` requests timed out in `artifacts/hardware/com16/rp2040_direct_mode3_diag_late_4b90291_actions_28639072595_COM16.json` and `artifacts/hardware/com16/rp2040_direct_mode3_ping_after_diag_timeout_4b90291_actions_28639072595_COM16.json`. That diagnostic was rejected as a bridge-responsiveness regression and reverted by commit `5f06eaf777841673f9b178572ed273c61a48bfde`. Actions run `28639309122` passed all jobs for the revert; the downloaded bridge UF2 SHA-256 `040D90497EE2300B48E82A1F23AC382F83639A1743EDC513A2824CA13ABF42C8` was flashed to COM16 and `artifacts/hardware/com16/rp2040_restored_ping_mount_5f06eaf_actions_28639309122_COM16.json` proves `DESKOS_SD_PING` responds with `sd_touch=0` while `DESKOS_SD_MOUNT` still reports `state=error note=sd_probe_rejected_card probe_err=3 probe_data=0 detect=low`. No SD formatting, Public RF, COM11, or COM29 access occurred.
- Keep using the operator-confirmed D1L validation port after RP2040/ESP32 changes; only run SD file/export/map canaries once `storage status` reports `state="ready"`, `file_ops=true`, and `atomic_rename=true`.
- Complete SD card acceptance so `storage status` reports the ready file-operation gates and `storage filecanary` proves temp write, read, rename replace, stat, final read, and cleanup against a real card.
- Complete hardware proof for retained Public/DM history, route history, packet history, diagnostic exports, sampled data exports, and map-tile cache once a card is electrically detected.
- Implement the full map page/tile download policy on top of the SD cache path.
- Keep settings, identity, and minimum boot-critical state on onboard storage.
